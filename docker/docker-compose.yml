services:
    agent:
        image: bugfix-ci:dev
        build:
            context: ..
            dockerfile: docker/Dockerfile
        volumes:
            - ../:/workspace
        env_file:
            -   ../.env
        environment:
            CONFIG_FILE: /workspace/config/bugfix.yml
